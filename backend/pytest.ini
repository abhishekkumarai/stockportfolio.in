[pytest]
testpaths = tests
# The app package is imported as `app.*`, so the backend directory must be on
# sys.path. There are no __init__.py files, so rootdir insertion does the job.
pythonpath = .
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
