// Tailwind v4 is the engine behind shadcn/ui. The project's own obsidian theme
// lives in globals.css and is layered on top; Tailwind adds the utility layer
// the shadcn components are written against.
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
